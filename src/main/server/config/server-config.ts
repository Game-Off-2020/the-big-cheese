import { CommonServerConfig } from './common-server-config';

export class ServerConfig extends CommonServerConfig {
   static readonly MOON_RADIUS: number = 1000;
   static readonly MAX_NR_OF_PLAYERS: number = 20;
}
